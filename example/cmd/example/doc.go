/*
Package `main` is an example of how an external repo might use Informer. This `main` package shows how a CLI (aka. command) creates an InfoService based on the global variables imported from the root package of the example project.
*/

package main
